export {
  SETTINGS_KEY,
  getSettings as getFolderSettings,
  normalizeSettings as normalizeFolderSettings,
  updateSettings as updateFolderSettings,
} from '@/src/core/settings';
export type {
  AppSettings as FolderSettings,
  FormulaClickAction,
  FolderItemDropAction,
  FormulaCopyFormat,
} from '@/src/core/settings';
